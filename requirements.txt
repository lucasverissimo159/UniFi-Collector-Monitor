# UniFi Collector Monitor v3.0 - Dependencies
# Sistema Inteligente Completo

# Interface Gráfica
PyQt5>=5.15.0

# Requisições HTTP (UniFi API)
requests>=2.28.0
urllib3>=1.26.0

# Nota: Todas as outras dependências são da biblioteca padrão do Python:
# - json (persistência de dados)
# - hashlib (criptografia SHA-256)
# - datetime (timestamps)
# - subprocess (ping)
# - concurrent.futures (threading)
# - os, sys, re, platform (utilitários)
